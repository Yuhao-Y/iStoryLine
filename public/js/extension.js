function createExtension(sceneNumber) {
	d3.select(".storyBoard").select("svg").selectAll("rect").selectAll(
        function() {
            
        }
    )
}

function createExtensionBySceneNumber(sceneNumber) {

}
